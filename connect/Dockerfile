FROM confluentinc/cp-kafka-connect:7.5.1
# Install a simple generator to verify Connect works.RUN confluent-hub install --no-prompt confluentinc/kafka-connect-datagen:0.6.4E
git add docker-compose.yml connect/Dockerfile
git commit -m "feat: Kafka in KRaft + Connect (datagen) + Kafka-UI"

